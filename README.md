# 📱 PhotoRevive

------

## 📦 项目简介

**PhotoRevive**是一个使用 **Kotlin** 和 **Android Gradle Plugin (AGP)** 构建的仿“醒图app” Android 应用。采用 **Kotlin DSL** 编写构建脚本（`build.gradle.kts`），采用现代 Android 开发架构，包含多个功能模块，提供流畅的用户体验。

主要功能

- 🖼️ **相册浏览**：展示本地照片库
- ✨ **AI 灵感推荐**：智能生成修复/美化建议
- ✂️ **照片编辑**：基础修图与增强功能
- 👤 **个人中心**：用户设置与作品管理
- 🎞️ **启动页 & 引导 Banner**：沉浸式启动体验

技术栈

- **语言**：Kotlin
- **架构**：MVVM（通过 Fragment + Activity 组织）
- **UI 框架**：Jetpack Compose（如适用）或传统 XML 布局
- **导航**：Bottom Navigation + Fragment
- **资源管理**：多分辨率 mipmap（hdpi ~ xxxhdpi）、主题适配（含深色模式）

------

## 🛠️ 构建与运行

### 前提条件

- Android studio版本：**Android Studio Narwhal | 2025.1.1** **Patch** **1**
- JDK 17（与 AGP 兼容版本）
- Android SDK（包含所需 API 级别）

> 安装 Android Studio后，通常 JDK 和 SDK 会自动配置好，无需额外设置。

#### 获取项目代码

打开终端（Terminal / PowerShell / CMD），执行以下命令克隆项目：

```cmd
git clone https://github.com/400lai/PhotoRevive.git
cd PhotoRevive
```



#### 运行应用

##### 方法一：通过 Android Studio（推荐）

1. 确保已连接 **Android 设备（开启 USB 调试）** 或启动 **AVD 模拟器**
2. 在工具栏顶部选择设备和 `app` 模块
3. 点击绿色 **Run** 按钮
4. 应用将自动安装并启动，首屏为 **SplashActivity**

##### 方法二：通过命令行

在项目根目录下执行：

```cmd
# 构建 Debug 版本 APK
./gradlew assembleDebug

# 安装到已连接的设备（需 adb 可用）
adb install app/build/outputs/apk/debug/app-debug.apk

# 或直接构建并安装（等效于点击 Run）
./gradlew installDebug
```

> 💡 Windows 用户请将 `./gradlew` 替换为 `gradlew.bat`



#### 构建发布版本（APK/AAB）

```bash
# 生成签名 APK（需先配置 signingConfigs）
./gradlew assembleRelease

# 生成 AAB（用于 Google Play）
./gradlew bundleRelease
```

------

## 📁 项目结构概览

```
app/
├── src/
│   ├── androidTest/                     # 仪器化测试代码
│   ├── main/
│   │   ├── AndroidManifest.xml          # 应用清单文件
│   │   ├── java/com/laiiiii/photorevive/
│   │   │   ├── MainActivity.kt          # 首页入口 Activity
│   │   │   ├── activity/
│   │   │   │   ├── AlbumActivity.kt     # 相册页面
│   │   │   │   ├── EditorActivity.kt    # 编辑器页面
│   │   │   │   └── SplashActivity.kt    # 启动页
│   │   │   ├── data/
│   │   │   │   └── repository/
│   │   │   │       └── LocalMediaRepository.kt  # 本地媒体数据仓库（基于 MediaStore）
│   │   │   └── ui/
│   │   │       ├── custom/
│   │   │       │   └── ShimmerCardView.kt       # 自定义 View：加载骨架屏效果
│   │   │       ├── editor/
│   │   │       │   ├── EditorState.kt           # 编辑器状态管理
│   │   │       │   ├── EditorTouchListener.kt   # 编辑画布手势监听
│   │   │       │   └── EditorViewModel.kt       # 编辑器视图模型（用于 MVVM）
│   │   │       ├── album/
│   │   │       │   ├── AlbumViewModel.kt        # 相册页面视图模型
│   │   │       │   └── AlbumAdapter.kt           # 相册页面的 RecyclerView 适配器
│   │   │       ├── preview/
│   │   │       │   ├── PreviewActivity.kt       # 预览页面
│   │   │       │   ├── PreviewViewModel.kt      # 预览页面视图模型
│   │   │       │   └── PreviewAdapter.kt        # 预览页面的 RecyclerView 适配器
│   │   │       └── widgets/
│   │   │           ├── CustomButton.kt          # 自定义按钮
│   │   │           ├── LoadingIndicator.kt      # 加载指示器 View
│   │   │           └── ImageViewWithPlaceholder.kt # 带占位符的 ImageView
│   │   └── res/                         # 资源文件
│   │       ├── drawable/                # 图标、形状等 drawable 资源
│   │       ├── layout/                  # 布局文件
│   │       ├── menu/bottom_nav_menu.xml # 底部导航菜单
│   │       ├── values/                  # 字符串、颜色、主题等
│   │       └── mipmap-*/                # 应用图标（含 WebP 格式支持）
│   └── debug/
│       └── kotlin_module           # 调试构建专用模块（Kotlin）
├── build.gradle.kts                # 模块级构建脚本（含 R8/Proguard 配置）
└── proguard-rules.pro              # 代码混淆规则
```

------

## 📜 许可证

本项目采用 [MIT License](https://www.qianwen.com/chat/LICENSE)（请根据实际情况替换）。

------



